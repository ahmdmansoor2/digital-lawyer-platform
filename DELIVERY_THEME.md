# تسليم دعم الوضع الفاتح والداكن — منصة المحامي الرقمية

أضيف إلى المشروع دعم متكامل للوضعين الفاتح والداكن مع زر تبديل واضح، مع حفظ اختيار المستخدم في `localStorage` تحت المفتاح `mohamidigital_theme`. عند أول زيارة، يُحترم إعداد النظام `prefers-color-scheme`، ثم يصبح الاختيار اليدوي هو المفضل في الزيارات اللاحقة.

## ما تم تنفيذه

أُنشئ hook مشترك في `src/hooks/usePublicTheme.ts` لتطبيق الثيم على عنصر `html` مبكراً، وتُنشئ مكوّنات الصفحة العامة وشاشة الدخول زر تبديل قابل لإعادة الاستخدام في `src/components/PublicThemeToggle.tsx`. أُضيف الزر إلى الصفحة الرئيسية `/` وشاشة دخول المنصة `/app` دون تغيير أي منطق للمصادقة عبر Google أو البريد الإلكتروني أو الهاتف وOTP.

أُضيفت قواعد CSS للواجهة العامة في `src/index.css`، مع الحفاظ على نظام الثيمات الداخلي الحالي (`theme-*`) مستقلاً. كما أضيفت تهيئة مبكرة في `index.html` لمنع وميض الثيم أثناء تحميل React.

بالنسبة للصفحات HTML الثابتة، أُضيف `public/theme-toggle.js` و`public/public-theme.css`، وأُدرجت الأصول المشتركة في الصفحات التي تستخدم الرأس العام. يشمل الوضع الفاتح الخلفية والبطاقات والعناوين والتذييل، وليس زر الرأس فقط.

## نتائج التحقق

| الفحص | النتيجة |
|---|---|
| `npm run type-check` | ناجح |
| `npm test -- --run` | ناجح — 235 اختباراً في 16 ملفاً |
| `npm run build` | ناجح |
| اختبار بصري `/` | زر التبديل يعمل في الاتجاهين |
| اختبار بصري `/app` | زر التبديل ظاهر ووسائل المصادقة محفوظة |
| اختبار صفحة ثابتة | زر التبديل والألوان يعملان عبر `public-theme.js` و`public-theme.css` |

يظهر تحذير البناء المعتاد الخاص ببعض الحزم الكبيرة بعد التصغير، لكنه لا يمنع نجاح البناء ولا يرتبط بتعديل الثيم.

## ملفات التسليم

يتضمن ملف ZIP نسخة المصدر مع التعديلات، مستبعداً `node_modules` و`dist` و`.git` وملفات البيئة والأسرار. ويتضمن ملف التصحيح الفروقات القابلة للتطبيق على نقطة المقارنة الحالية (`025a87d`).

## النشر على جهاز المستخدم

بعد نسخ الملفات إلى المستودع المحلي على جهاز Windows، يُنصح بمراجعة `git diff` ثم تنفيذ:

```bash
git add index.html public src scripts
git commit -m "feat: add light and dark theme toggle"
git push origin main
firebase deploy --only hosting:app
```

إذا استُخدم ملف التصحيح بدلاً من نسخ الحزمة، يمكن تطبيقه من جذر المشروع بواسطة:

```bash
git apply mohamidigital_theme.patch
```

يجب عدم رفع أي ملف `.env` أو مفاتيح API أو بيانات اعتماد. بعد النشر، اختبر `https://mohamidigital.online/` و`https://mohamidigital.online/app` في نافذة خاصة، ثم بدّل الثيم وانتقل بين الصفحات للتأكد من حفظ التفضيل.
